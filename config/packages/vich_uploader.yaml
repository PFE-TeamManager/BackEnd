vich_uploader:
    db_driver: orm
    # u can modify this a s u need 
    mappings:
        images:
            uri_prefix: /images
            upload_destination: '%kernel.project_dir%/public/images'
            # Decide how uploaded files would be renamed
            namer: Vich\UploaderBundle\Naming\UniqidNamer
